<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net472</TargetFramework>
    <UseWPF>true</UseWPF>
    <AssemblyName>WebPackageLicenseGenerator</AssemblyName>
    <RootNamespace>WebPackageLicenseGenerator</RootNamespace>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\WebPackageViewer\WebPackageViewer.csproj" />
    <Reference Include="System.Security" />
  </ItemGroup>

</Project>